dist: xenial
language: go

go:
  - "1.10.x"
  - "1.11.x"

script:
  - go test -v ./...
  - go build
  - ./bench.sh ./specious-db -entries 10000

addons:
  apt:
    packages:
      - libleveldb-dev
      - libleveldb1v5
