dist: xenial
language: go

go:
  - "1.10.x"
  - "1.11.x"
  - "1.6.x"

script:
  - go test -v ./...
  - go build -v
  - ./bench.sh ./specious-db -entries 10000

addons:
  apt:
    packages:
      - libleveldb-dev
      - libleveldb1v5
